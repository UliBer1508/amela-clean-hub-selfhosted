## Umsetzung

In `src/pages/Calendar.tsx`:

1. **Mobile Header**: gesamte Navigation-Zeile (`← Heute →`) im `sm:hidden` Block entfernen. Titel und View-Switcher bleiben.
2. **Card-Header (Mobile)**: einen kompakten `Heute`-Button zwischen den beiden Pfeilen einfügen. Auf Desktop unverändert.
